-define(THRESHOLD, 20).

-define(TOLERANCE,10).
-define(MIN_LEN,7). % a minimal length of the string that should be matched
-define(MAX_RANGE,25).
-define(MAX_LEN,17). % a maximal length of the string after which the range should be less than MAX_RANGE

-define(SORTED_MAX,1000000). %maximal length of array to sort

